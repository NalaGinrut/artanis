* GNU Artanis doesn't support RFC2324, it's a terrible bug, but it's never supported in the foreseeable future, sadly. (I'm serious, so don't remove this line)
* keep-alive under HTTP/1.1 will crash for continuous request
